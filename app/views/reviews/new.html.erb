<h1><%= new_review_header %></h1>

<%= render partial: "layouts/errors", locals: {object: @review}%>

<%= form_for([@review.movie, @review]) do |f|%>
<% if !@review.movie %>
  <%= f.label :movie %>
  <%= f.collection_select :movie_id, Movie.all, :id, :title %>
<% else %>
  <%= f.hidden_field :movie_id %>
<% end %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <%= f.submit %>
<% end %>

